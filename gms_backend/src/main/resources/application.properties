spring.application.name=gms
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/gms
spring.datasource.username=gms_user
spring.datasource.password=gms_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration (disabled for local run due to version issue)
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

# Actuator
management.endpoints.web.exposure.include=health,info

# Mail (SMTP) Configuration (temporarily hardcoded for local/prod testing)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=imshubhy@gmail.com
spring.mail.password=lgtlbhjphkfimpmv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=imshubhy@gmail.com

# Security Enhancements
server.error.include-message=never
spring.jpa.open-in-view=false
spring.servlet.multipart.enabled=false

# Logging Configuration
logging.level.com.gms=DEBUG
logging.level.com.gms.config.SuperAdminBootstrapRunner=DEBUG
logging.level.com.gms.service.bootstrap.SuperAdminBootstrapService=DEBUG